# Force the CPU-only torch wheel (~190MB) instead of the default CUDA build (~2.7GB).
# The full CUDA wheel blows past Railway's build image/memory limits -> build failure.
# sentence-transformers pulls torch transitively; pinning it here resolves it from
# the PyTorch CPU index before that happens.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-dotenv==1.0.1
gitpython==3.1.43
chromadb==0.5.23
sentence-transformers==3.3.1
anthropic>=0.40.0
sse-starlette==2.1.3
pydantic==2.10.3
tiktoken==0.8.0
httpx==0.28.1
